services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./AIbuild/output3:/app/AIbuild/output3:ro
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
